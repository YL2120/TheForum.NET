@page
@model SetPasswordModel
@{
    ViewData["Title"] = "Set password";
    ViewData["ActivePage"] = ManageNavPages.ChangePassword;
}

<h3>Set your password</h3>
<partial name="_StatusMessage" for="StatusMessage" />
<p class="text-info h3">
    You do not have a local username/password for this site. Add a local
    account so you can log in without an external login.
</p>
<div class="row">
    <div class="col-md-6">
        <form id="set-password-form" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger h3"></div>
            <div class="form-floating">
                <input asp-for="Input.NewPassword" class="form-control h3" autocomplete="new-password" />
                <label asp-for="Input.NewPassword" class="form-label h3"></label>
                <span asp-validation-for="Input.NewPassword" class="text-danger h3"></span>
            </div>
            <div class="form-floating">
                <input asp-for="Input.ConfirmPassword" class="form-control h3" autocomplete="new-password" />
                <label asp-for="Input.ConfirmPassword" class="form-label h3"></label>
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger h3"></span>
            </div>
            <button type="submit" class="w-25 h3 btn btn-lg btn-primary">Set password</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
